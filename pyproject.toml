[project]
name = "imc-classifier-python-lab"
version = "0.1.0"
description = "Pure Python and pandas lab for BMI calculation, classification, and optional CSV input."
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Alberto Mateus P. da Gama" }]

[tool.pytest.ini_options]
pythonpath = ["."]
